schema_version = "1.3"

[task]
name = "disteval/hard-algorithm"
description = "Implement an efficient solution to a graph shortest-path variant with constraints"
keywords = ["python", "algorithms", "graphs", "hard"]

[metadata]
difficulty = "hard"
difficulty_explanation = "Requires algorithmic insight, correct handling of edge cases, and performance constraints"
category = "algorithms"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 300.0

[environment]
build_timeout_sec = 120.0
