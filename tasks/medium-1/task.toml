schema_version = "1.3"

[task]
name = "disteval/medium-log-parser"
description = "Parse and analyze a log file, extract statistics"
keywords = ["python", "parsing", "medium"]

[metadata]
difficulty = "medium"
difficulty_explanation = "Requires parsing, aggregation, and correct output format across multiple fields"
category = "data-processing"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 180.0

[environment]
build_timeout_sec = 120.0
